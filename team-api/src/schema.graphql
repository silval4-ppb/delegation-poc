type Member {
  id: ID!
}

type Team {
  id: ID!
  name: String!
  members: [Member!]!
}

type Query {
  teams: [Team!]!
}

query GetTeams {
  teams {
    name
    members {
      name
      id
    }
  }
}

query getTeamMembers {
  membersByTeam(teamId: 1) {
    id
    name
  }
}
